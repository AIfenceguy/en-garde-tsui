// Copy this file to js/lib/config.js and fill in your Supabase project details.
// The anon key is safe to commit (RLS protects data). The service_role key is NOT — never put it here.

export const SUPABASE_URL = 'https://YOUR-PROJECT-REF.supabase.co';
export const SUPABASE_ANON_KEY = 'eyJ...';

// Optional: set a custom site URL for Google OAuth redirect override.
// Defaults to window.location.origin + path.
export const REDIRECT_TO = null;
